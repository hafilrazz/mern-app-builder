import React from 'react';
import { NavLink } from 'react-router-dom';
import { LayoutDashboard, Database, Table, Layers } from 'lucide-react';

export default function Sidebar() {
  const navLinkClass = ({ isActive }) =>
    `flex items-center space-x-3 px-3 py-2.5 rounded-lg text-sm font-medium transition-all ${
      isActive
        ? 'bg-blue-50 text-blue-700 font-semibold shadow-xs'
        : 'text-gray-600 hover:bg-gray-100 hover:text-gray-900'
    }`;

  return (
    <aside className="w-64 border-r border-gray-200 bg-white flex flex-col justify-between h-[calc(100vh-4rem)] sticky top-16">
      <div className="p-4 space-y-6">
        <div>
          <p className="px-3 text-xs font-semibold uppercase tracking-wider text-gray-400">
            Overview
          </p>
          <div className="mt-2 space-y-1">
            <NavLink to="/" end className={navLinkClass}>
              <LayoutDashboard className="h-4 w-4" />
              <span>Dashboard</span>
            </NavLink>
          </div>
        </div>

        <div>
          <p className="px-3 text-xs font-semibold uppercase tracking-wider text-gray-400">
            Entities
          </p>
          <div className="mt-2 space-y-1">
            {{#each entities}}
            <NavLink to="/{{lowercase (pluralize name)}}" className={navLinkClass}>
              <Table className="h-4 w-4" />
              <span>{{capitalize (pluralize name)}}</span>
            </NavLink>
            {{/each}}
          </div>
        </div>
      </div>

      <div className="p-4 border-t border-gray-100">
        <div className="rounded-lg bg-blue-50 p-3">
          <div className="flex items-center space-x-2 text-blue-700">
            <Layers className="h-4 w-4" />
            <span className="text-xs font-semibold">Generated App</span>
          </div>
          <p className="mt-1 text-xs text-blue-600/80">
            Powered by MERN App Builder
          </p>
        </div>
      </div>
    </aside>
  );
}
